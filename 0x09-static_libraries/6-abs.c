#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n : number
 *
 * Return: n if greater than 0, -n otherwise
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
